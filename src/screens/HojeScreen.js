// ============================================================
// src/screens/HojeScreen.js
// Tela "Hoje" — primeira aba do app.
//
// Por enquanto é um placeholder. Quando formos construir
// essa tela de verdade, todo o conteúdo vai entrar aqui.
// ============================================================

import React from 'react';
import PlaceholderScreen from '../components/PlaceholderScreen';

export default function HojeScreen() {
  // Renderiza a tela temporária com o título "Hoje"
  return <PlaceholderScreen title="Hoje" />;
}
